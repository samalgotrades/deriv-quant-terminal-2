# Signal Service

The MVP signal implementation currently lives in `apps/api/src/signals`. Split it into this service when signal processing needs independent scaling or a separate deployment lifecycle.
